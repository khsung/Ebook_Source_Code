/*
#include<stdio.h>

void recursive(int num) {
	if (num == 0) {     //종료 조건
		printf("count 종료");
	}
	else {
		printf("count : %d\n",num);
		recursive(num-1);
	}
}

int main() {
	int num = 5;
	recursive(num);
}
*/
