/*
#include<stdio.h>

int recursive(int num) {
	if (num == 0) {     //종료 조건
		printf("count 종료\n");
		return 0;
	}
	else {
		printf("count : %d\n",num);
		return num+recursive(num-1);
	}
}

int main() {
	int num = 5;
	printf("총 걸린 시간 : %d",recursive(num));
}
*/
